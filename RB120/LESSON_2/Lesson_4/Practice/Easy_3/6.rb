class Cat
  attr_accessor :type, :age

  def initialize(type)
    @type = type
    @age  = 0
  end

  def make_one_year_older
    self.age += 1
  end
end

# we could simply increment the instance variable @age itself as opposed to incrementing the value
# through its corresponding setter method. 